<system>
  log_level debug
</system>

<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type stdout
  format json
  time_format %Y-%m-%dT%H:%M:%S.%L%z
  include_tag_key true
  tag_key tag
</match>